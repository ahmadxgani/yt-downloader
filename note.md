# Tutorial update package in https://www.npmjs.com/package/youtube-downloader-cli

```bash
git commit -am "your message"
git push
npm version patch
npm publish
```

# Tutorial update global package

```bash
sudo npm -g update youtube-downloader-cli
```
